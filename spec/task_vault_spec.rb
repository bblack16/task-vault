require 'spec_helper'

describe TaskVault do
  it 'has a version number' do
    expect(TaskVault::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
